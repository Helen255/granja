@layout('layouts.app')
 @section('content')
  
  <div class="container">
    <div class="row mt-5">
      <div class="col-3 offset-9">
        <a href="/webFase/recibir" class="btn btn-primary text-right">Ingresar</a>
      </div>

    </div>
    <div class="box">
     <h1 class="display-4 text-center">Fases</h1>

      <table class="table table-sm">
        <thead class="text-dark border border-info">
          <tr>
            <th scope="col">No</th>
            <th scope="col">Número Fase</th>
          </tr>
        </thead>
        <tbody>
          @each(so in data)
          <tr>
            <td>{{so.id}}</td>
            <td>{{so.numero_fase}}</td>

            <td>
              <div class="field is-grouped">
                <div> 
                  <a href="/webFase/editar/{{so.id}}"  class="btn btn-warning text-right">Editar</a>
                  <a href="/webFase/elimina/{{so.id}}"  class="btn btn-danger text-right">Eliminar</a>
                  <a href="/web/detalle/{{so.id}}"  class="btn btn-success text-right">Detalle</a>
                </div>
              </div>
            </td>
          </tr>
          @endeach
        </tbody>
      </table>
    </div>  
  </div>
@endsection
    
    

